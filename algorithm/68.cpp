#include<stdio.h>
#include<vector>

using namespace std;

vector<int>adlist[21];
int n, m, cost = 10000;
int check[30];

int main() {
	int i, a, b, c;
	scanf("%d %d", &n, &m);

	for (i = 1; i <= m; i++) {
		scanf("%d %d %d", &a, &b, &c);

	}

}